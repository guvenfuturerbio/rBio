import 'package:flutter/material.dart';

class QueryParametersError extends StatelessWidget {
  const QueryParametersError({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Hata Oluştu.'),
    );
  }
}
